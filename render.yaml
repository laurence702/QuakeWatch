services:
  - type: web
    name: earthquake-dashboard
    env: docker
    # IMPORTANT: Replace the repo URL with the URL of your own repository
    repo: https://github.com/laurence702/QuakeWatch
    plan: free
    branch: main
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: 10000
      - key: ENVIRONMENT
        value: production
